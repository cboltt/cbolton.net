import React from "react";
import ReactDOM from "react-dom/client";
import Birthday from "./Birthday/Birthday.tsx";
import "./index.scss";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <Birthday />
  </React.StrictMode>
);
